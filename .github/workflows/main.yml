name: Android14-6.1-KO
on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Cache kernel
        id: cache-kernel
        uses: actions/cache@v4
        with:
          path: android-kernel
          key: android14-6.1-kernel-cache

      - name: Install repo
        run: |
          curl https://storage.googleapis.com/git-repo-downloads/repo > /usr/local/bin/repo
          chmod a+x /usr/local/bin/repo

      - name: Repo init kernel
        if: steps.cache-kernel.outputs.cache-hit != 'true'
        run: |
          mkdir -p android-kernel
          cd android-kernel
          repo init -u https://android.googlesource.com/kernel/manifest -b common-android14-6.1 --depth=1
          repo sync -j32 --no-clone-bundle --no-tags

      # 把驱动放进内核目录
      - name: Add driver
        run: |
          cd android-kernel
          mkdir -p common/drivers/hello
          cp ../hello.c ../Makefile common/drivers/hello/
          echo "obj-m += hello/" >> common/drivers/Makefile

      # ==========================================
      # 👇 👇 👇 真正只编译 ko，不编译内核！
      # ==========================================
      - name: 只编译 hello.ko 模块
        run: |
          cd android-kernel
          tools/bazel build --config=fast //common/drivers/hello:modules

      # 自动找到 ko 并上传
      - name: Upload ko
        uses: actions/upload-artifact@v4
        with:
          name: hello-ko
          path: android-kernel/out/android14-6.1/common/drivers/hello/*.ko
